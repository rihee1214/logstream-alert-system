# 작업 해야할 목록

#task
- [ ] git, github branch 전략 고민하기
- [ ] checkstyle 제대로 동작 안하는 현상 고쳐야함
- [ ] Mockup 영역 작업 필요 사항
	- [x] 여러가지 복잡한 흐름 로직 작성
	- [ ] 스케쥴러를 통해 일정 주기마다 요청하는 로직 작성
	- [ ] 요청하여 실패, 성공을 트리거 하거나, 여러가지 명령을 줄 수 있는 로직 작성
	- [x] Exeption을 내놓도록 하여 오류시 stacktrace도 나오는지 확인
- [ ] 2025-07-01 고려사항
	- [ ] logging service와 alert rule service를 합칠지 말지에 대한 고민
		- [ ] alert rule service의 정책이 DB에 저장되는 유형의 것 이라면 합쳐도 좋을거같음
		- [ ] 자주 변경되고, 핵심 도메인에 가깝다면 logging 은 합치지 않는것이 좋아보이고, 사실상 내부적으로 여러개의 동작이 들어있는 편이기도함
	- [X] alert rule service에 대한 정책 고민
		- [X] DB에 저장할 것인지
			- [X] DB에 저장하면, 결국 정책의 변경으로 인한 슬라이딩 윈도우 변환도 문제고, 계속 DB를 보면서 활동하게 할 수도 없는 노릇임
		- [X] 코드와 properties로 저장할 것인지
- [ ] 2025-08-01 ~ 03
	- [ ] logging-service의 문제 상황을 빠르게 판단하기 위해 메트릭 제공 코드 작성 및, 로그 전송에 대한 고려 필요
	- [X] kafka key에 서비스,호스트 이름, 트랜잭션 아이디 등을 넣어서 어디에서 문제가 발생하였는지 알 수 있는 단초를 제공해야한다. (단초만 제공한다. 애초에 근본적인 원인을 확인하는것은 불가능하다. producer가 잘못되어있기 때문에 로그 수집기 입장에서는 해당 서비스에 무슨 문제가 있는지 알 방도가 없고, 그 역할도 가질 수 없다.)
	- [x] 기존에 `List<LogMessage>`로 두었던 LogProcessor의 출력과 입력을 LogProcessingContext를 인터페이스로 바꾼다. 그리고 pipeline안에 context패키지를 넣어서 그 구현체들을 넣도록 한다.
- [ ] 2025-08-06
	- [X] logging-service 각 단계를 ProcessResult로 처리하도록 함. 모든 Exception에 대해서 catch하여 worker의 동작에 영향이 없도록 할 것.
	- [x] 자원 회수에 대한 빡센 고찰이 필요하다. catch문을 넣고, 모든 자원에 대해서 runtime에 shutdownHook를 걸어서 회수하도록 만든다.
	- [ ] kafkaCollector에서 메시지를 가져올 때 messageId가 없으면, 생성할 수 있는지 확인하고, 생성할 수 있다면, 생성한 후, 성공 메시지 이력에 추가 필드를 넣어 생성된 키라는 boolean값을 넣어 저장할 것. 만약 생성할 수 없다면 그냥 로그만 찍고 넘어갈 것.
	- [ ] 에러 로그를 저장하기 위한 스키마와 정상 로그를 저장하기 위한 스키마를 common영역의 constants 영역에 만들어 둘 것. 그 이유는 해당 요소는 monitoring service에서도 직접적으로 사용할 예정이기 때문이다.
- [ ] 2025-08-07
	- [ ] 테이블 생성 코드
		- [ ] 에러 테이블
		``` postgres
		CREATE TABLE err_logs (
	    message_id     VARCHAR(64) PRIMARY KEY,
	    log_version    VARCHAR(16) DEFAULT '1.0',
	    origin_log     TEXT        NOT NULL,   -- 원문 그대로 저장
	    reason         TEXT,                   -- 실패 사유
	    occurred_at    TIMESTAMP DEFAULT NOW() NOT NULL -- 수신 시각 기록
		);
		
		CREATE INDEX idx_err_logs_occurred_at ON err_logs(occurred_at);
		```
		- [ ] 일반 테이블
		```postgres
		CREATE TABLE normal_logs (
		  message_id            VARCHAR(64) PRIMARY KEY,
		  schema_version_major  SMALLINT    NOT NULL DEFAULT 1,
		
		  logtype               VARCHAR(8)  NOT NULL,
		  occurred_at           TIMESTAMP   NOT NULL,
		  level                 VARCHAR(8)  NOT NULL,
		  service               VARCHAR(100) NOT NULL,
		
		  host                  VARCHAR(255) NOT NULL,
		  container             VARCHAR(255) NOT NULL,
		
		  trace_id              VARCHAR(64),
		  span_id               VARCHAR(32),
		
		  -- 변화 잦고 상세한 것들은 버킷으로
		  ctx_call              jsonb,   -- 초기엔 JSON 문자열; 나중에 DB별 json/jsonb
		  meta                  jsonb,
		
		  created_at            TIMESTAMP NOT NULL DEFAULT NOW()
		);
		
		-- 인덱스(상황에 맞게)
		CREATE INDEX idx_nl_ts       ON normal_logs("timestamp");
		CREATE INDEX idx_nl_service  ON normal_logs(service);
		CREATE INDEX idx_nl_host     ON normal_logs(host);
		CREATE INDEX idx_nl_container ON normal_logs(container);
		CREATE INDEX idx_nl_trace    ON normal_logs(trace_id);
		```
---
# 이미 작업한 목록
#done
- [x] docs 템플릿 Obsidian화
- [x] 사용하고 있는 tool에 대한 목록 README 정리
	- [x] JDK (openjdk 21)
	- [x] IDE (intellij)
	- [x] Obsidian
- [x] 기존 로그 구조 리팩토링 필요성 분석 및 로거 인터페이스 분리 (새로운 로거 추가 및 정책 변경시 변경의 용이성을 획득하기 위함)
- [x] StructuredLoggerImpl class는 외부에서는 보이지 않고 interface를 통해 사용하도록 해야함.
	- [x] 그렇게 하는 방법이 없어서 그냥 interface에 주석만 달았음
- [x] mockup을 제외한 나머지 모듈들이 오류가 발생했을때 로그를 어떻게 처리해야할지 고민하기
      (그저 alertmanager에게만 의존하기로 결정)
- [x] KafkaAppender를 제거하고 다시 FileBeat를 사용하도록 README.md파일 수정
	- [x] 기술 스택에 다시 등록하기
	- [x] 아키텍처 구성도 수정하기
	- [x] 내용 다시 읽어보고 다시 원래대로 구성하기
- [x] bizlog-contract, syslog-contract 수정하고 actlog-contract작성
- [x] SpanLabelBeanPostProcessor의 postProcessAfterInitialization메서드 리팩토링
- [x] spanId 변경 관련 AOP, Interceptor 구조 재검토
	- [x] 인터셉터 관련 코드 작성
	- [x] 인터셉터 관련한 테스트 모듈 작성
	- [x] common영역의 AOP관련된 코드 세개 정리필요
- [x] actuator 코드 작성하기 (모든 시스템 전용)
      (property 세팅만 잘 되어있으면  됨)
- [x] Actuator는 localhost에서만 call할 수 있다던가 하는 설정 추가할 수 있게 만들기
- [x] 모든 환경 변수, property 세팅의 default값은 `__UNDEFINED__`임을 기록하고, 그것을 따로 뺄 궁리하기
- [x] google java 스타일의 checkstyle 적용으로 인해 .editorconfig 설정 바꿔서 에디터 설정 바꿔주기
	- [x] 기본 구성 완료
	- [x] 더 확장해서 자세한 설정 추가 작성하기
- [x] Actuator call 로그 남기게 만들기
- [x] 비지니스 서비스 호출에 대한 B3Header에 관련된 내용 추가하기.
- [x] bizlog-contract문서에 meta영역에  B3Header관련 내용 추가하기
	- [x] Meta영역에 sampled와 flags 요소가 들어가게됨
- [x] application.properties 문서에 주석 다듬기
	- [x] Common 영역
		- [x] service명(service.name) 넣어주어야 함
		- [x] application.properties에 monitoring.scheduler.interval.ms를 넣어주어야. actuator call 간격 설정 가능함을 알려야함
		- [x] Actuator 설정 관련 문서 남기기 (application.properties에 들어갈 항목 넣어주어야 함)
- [x] scheduleing관련된 코드 및 기존 테스트 코드 다듬기
- [x] properties는 여러 이유로 polling 방식을 사용하도록 결정
	- [x] 문서화 필요(WatchService 포기)
		- [x] 가상 컨테이너 환경에서 적절하게 동작하지 않을 가능성 높음
		- [x] 파일이 크지 않기 때문에 그리 부담되는 작업이 아님
- [x] prometheus wrapping service 만들기로 한 것에 대한 결정 문서 만들기
- [x] Actuator Scheduler는 common영역에서 오로지 health만 call하도록 결정함
	- [x] 해당 결정 문서 완성
	- [x] 기존 결정문서 {05-13} 수정필요
	- [x] 전체 문서보고 수정
	- [x] 아키텍처 문서 수정해서 prometheus와 elastic search와 연결시키기
	- [x] 테스트 작성 완료하기
	- [x] ActuatorSecurity관련 고려 필요.
		- [x] common영역에서 정의한 것을 다른 모듈에서 재정의 할 수 있는지 알아봐야함(whitelist에 대한 관리 필요함)
- [x] 문서 작성 필요 요소
	- [x] Actuator Filter관련된 설명을 추가하기
	- [x] 개발 가이드 문서 조금 더 확장 가능하고 많은 내용을 품도록 구조 및 내용 수정하기
		- [x] common영역에 대한 문서 수정 내용(길어지면 링크로 참조. 서브 디렉토리에 넣기)
			- [x] 전체를 세가지 파트로 나누어 설명
				- [x] StructureLogger에 대한 설명과 logback-spring 형식으로 전체 로그 구조 설명
				- [x] 인터셉터에 관한 내용들과 그에 대한 주요 요소들 설명
				- [x] Actuator관련된 요소들과 그에 대한 주요 요소들 설명
		- [x] biz영역에 대한 문서 수정 내용(길어지면 링크로 참조. 서브 디렉토리에 넣기)
			- [x] 아직 영역을 나눌게 없지만, common영역에 대한 내용 몇 가지 추가해야함
				- [x] StructureLogger와 log구조 설명 및 log-level-semantics에 대한 설명 추가
				- [x] 로그를 처리하기 위한 StructureLogger 사용과 설명, 그리고 인터셉터에 대한 설명 추가
				- [x] Actuator scheduler에 대한 설명 및 기존 scheduler 교체 방안 제공
- [x] Common 영역 작업 필요 사항
	- [x] **property 검증을 위한 코드 작성하기 (@PostConstruct 통해 validate 코드 만들어서 검증, 없다면 실패로 만들어버리기)**
	- [x] property 검증에 대한 내용도 common 개발 가이드에 추가하고, biz문서에도 그것은 각자의 책임이라고 명시하기.
- [x] **05/26 고민내용 : 모든것을 MVP로 만들고, 추후 넣었으면 좋겠는 것들, 정책 혹은 코드는 문서로 남겨만 두자!** -> decision에 추가필요
- [x] 05/27 생각한 내용 (ZIPKIN은 수집의 요소도 들어가고 영속성 DB와는 영 맞지 않기 때문에 제거해야함)
- [x] java option으로 넣어주는 properties를 Spring 기동시 검증할 수 있도록 해야함.
	- [x] 다만 문제는 모든 모듈에서 각자가 가지고 있는 properties만 검증하게 하려면 main에서 호출하게 만드는 것은 문제가 있음 (의존 대상자의 validate는 되지 않기 때문)
- [x] Mockup서비스 구축
	- [x] controller, service, dao 단계로 동작하도록 처리
	- [x] db연결은 굳이 필요없고, db에서 select한 것처럼 동작하게 만들기.
	- [x] 한 controller 여러개의 controller를 호출하게 만들고 추적이 되는지 확인 필요
- [x] mockup 서비스의 webclient에서 응답 로깅시 req 헤더들을 넣지 않는 문제 해결하기
      (Actuator 참조)
- [x] 각 문서 폴더에 README.md 파일 추가
	- [x] 1. 해당 폴더에 잇는 모든 문서들이 무엇을 의미하는지, 폴더명의 의미가 무엇인지 기재하여 이해를 도움
	- [x] 2. 해당 폴더에 있는 모든 요소들에 대한 인덱스 역할을 한다. (폴더 하위로 파일 같은 식으로 hierarchy 식으로 기재하기)
- [x] 05/28 생각한 내용
	- [x] 길이필드는 32, 16의 배수가 될 수 있도록 설정할 수 있는 요소를 application.properties에 넣도록 고려
	- [x] 메타 영역의 생존 여부 고려
		- [x] 문서 작성
		- [x] 리팩토링으로 meta관련 모든 요소 제거
			- [x] interceptor 코드에서 meta 제거
			- [x] actuator 코드에서 meta 제거
		- [x] biz, act, sys log contract 문서 모두 수정하기
	- [x] TraceId 검증 정책 완화 고려(duration에 대한 고민 중 결국 spanId는 자기가 만들더라도, 추적성을 위해 검증은 간단하게 하여, 추적이라도 가능한 수준으로 만들 수 있게 해야함)
		- [x] 입력된 traceId가 없으면 생성
		- [x] logging service에서 traceId를 강하게 검증한 후 제대로 된 형식의 traceId가 아니면 새로 생성한 후, response헤더에 넣어 보내도록 한다. 그렇게 하여 상대가 로깅하게 만들어서 일말의 추적 가능성을 남긴다.
- [x] 05/25 생각한 내용
	- [x] meta영역의 정합성 고민 (필요하면 그냥 추가한 다음 logging service에서 정리할지 고민해봐야함.)
	- [x] webClient에서 MDC, header 세팅 관련된 요소(간단한 수준의 MDC 전파) 고민해야함
	- [x] spanId 정책 관련해서 spanId를 중복이 적도록 하기 위해 UUID를 추가할지 고민
- [x] 계약 문서와, biz service 개발 가이드에 webClient관련 내용 추가하기
- [x] 06/03 작업 후 남은 내용
	- [x] WebClient 관련 정책 문서화
	    - [x] 요청 처리: builder 내부 filter 사용해 MDC, B3 헤더 등 주입 정책 설명 추가
	    - [x] 응답 처리: `Mono<WebClientCallResult>`를 이용한 후처리 정책 설명 추가
	    - [x] 예외 처리: Mono.error 활용 방식과 Error 레벨 로깅 정책 반영
	    - [x] 응답 수신시, response헤더로 들어온 B3헤더 traceId를 remoteTraceId 항목으로 MDC세팅 후 로깅할 수 있도록 강조하기.
	- [x] StackTrace 출력 정책 정리
	    - [x] Error 레벨에서만 StackTrace 출력하도록 제한
	    - [x] StackTrace 정보량 줄이면서도 핵심 내용 유지하는 출력 방식 고안
	- [x] application.properties에 환경변수관련 내용 추가하기.
		- [x] tracing.traceId.multiplier : traceId 길이 설정(배수)
		- [x] tracing.spanId.multiplier : spanId 길이 설정(배수)
- [x] Mockup영역 문서화 고려하기
	- [x] mock.token 관련해서 요청시 최소한의 조치로 X-Auth-Token을 헤더에 넣어서 보내도록함
	- [x] @EnableWebSecurity을 설정해서 기본 security 설정은 무시하고, 직접 설정한 security가 작용되도록 해야 함
	- [x] 환경변수 설명 들어가야 하는 부분
		- [x] mockup.external.base-url (테스트를 위한 외부 요청 기본 URL주소)
	- [x] biz 개발자 가이드에 주의해야 할 사항 정의
		- [x] webClient, Future등을 사용하는 경우에 bizLog를 찍을때, traceId와 같은 MDC정보가 전파될 수 있도록 수동으로 처리해야 한다는 문단 추가하기
- [x] docker, docker-compose.yml, kubernetes.yml 파일에 주석과 환경 변수 넣을 요소
	- [x] Common 영역
		- [x] HOST, CONTAINER 명을 환경 변수로 넣어 주어야 함
		- [x] Actuator를 Call하는 prometheus 인증 헤더 토큰(환경변수 key값: monitoring.token)
	       (HTTP header = X-Monitoring-Token)을 환경 변수로 넣어야 한다는 내용 문서화 필요.
- [x] 06/07 작업 후 남은 내용
	- [x] common-component에 StructuredMonoWebClient에 대한 설명 및 참고 문서 추가하기.
	- [x] mockup-service-usage에 추후 테스트를 위해 알아야할 항목에 대한 정리 필요(스케쥴러 매니저 관련 내용)
- [x] 설계 고려 대상
	- [x] 비지니스 로그 저장 DB
	- [x] thanos 어떤 컴포넌트를 이용할 것인지
	- [x] 추적 모니터링 솔루션 어떤 것을 사용할 것인지
- [x] 06/10 갑자기 테스트가 안되는 상황 발생. mockup 서비스 문제 해결해야함
	- [x] spring boot starter가 제대로 안붙는거 같음. 우선 common 서비스는 잘 동작하고 있음
	- [x] 문제 해결 후 해야할 일
		- [x]  prometheus 관련 테스트 세팅 준비 및 구성 고민
		- [x] alertmanager 세팅 구성 고민
		- [x] metric정보 저장을 위해, 그리고 중복되는 기능 문제로 직접 구현한 서비스 만들기로 결정.(이쪽이 유연성은 더 좋다.)
- [x] prometheus call 테스트하는데 basicauth가 걸려서 그런가 403에러 발생
	- [x] 헤더는 잘 들어가고 있는거 확인함(wireshark로)
	- [x] basic auth로도 안됨 일단 다시 구조 확인해봐야 할듯
- [x] 📅 06/12 아키텍처 관련 문제점 발생
	- [x] ElasticSearch 관련 문제
		- [x] ❗ 메트릭 정보 저장 및 추적에 적합하지 않음
			- [x] 원인: Aggregation/Join/Flow 구조에 비효율적 → 상세 분석 예정
		- [x] ❗ 대량의 비즈니스 로그 분석에 제약 있음
			- [x] 30일 이상 장기 보존 + 흐름 유추가 어렵고, 비용도 큼 → 대안 검토
	  - [x] Prometheus 관련 문제
		- [x] ❗ ElasticSearch가 Prometheus 메트릭 백엔드로 적합하지 않음
			- [x] 이유: 메트릭 저장 방식이 다르고 효율성 저하
		- [x] ✅ Thanos, Cortex, Mimir 등 고려 대상
			- [x] 기능: 백업, 멀티 테넌시, 고가용성, 장기 저장 가능
			- [x] Trade-off: 복잡도 증가, 리소스 사용 증가 → 도입 시점 판단 필요
- [x] prometheus관련 문서 작성 필요
	- [x] 여러가지 보안 설정에는 해당 서비스가 가지고 있는 보안 정책에 더불어서 헤더로 X-Monitoring-Token을 가지고 있어야 한다고 명시할 것
- [X] Logging service 로직에 대한 고려
  - [X] 어떻게 해야 warn레벨의 요소를 적절하게 notification에 보낼 수 있을지.
  - [X] error는 바로 보내야겠지만, warn은 애매한 경우가 많을 수 있음
- [X] Elasticsearch에 저장된 데이터 백업 및 압축 저장 관련된 정책 고려
- [X] 중복 키를 적용하여 alermanager에 적용하는 방법과, kafka에 적용하는 방법 둘다 알아보기
- [X] 아키텍처 수정에 대한 고민
    - [X] logging-serivce의 역할 수정
        - [X] 들어온 로그를 재 구조화 한 후 elasticsearch에 적재.
        - [X] 이후 alerting 정책은 Notification service 추가하여 거기에 적재.
    - [X] Notification service 추가
        - [X] alerting (sms, email 같은 서비스) 서비스 제거
        - [x] 알림과 실패에 대한 처리를 여기서 맡게 됨
    - [X] alert policy service 추가
        - [X] logging-service와 notification service 사이에 존재하는 정책 서비스
        - [X] alerting에 대한 모든 전략을 가지고 있는 요소 추가
        - [X] prometheus 는 메트릭 전략, 해당 서비스는 비지니스 로직에 대한 알림 전략
        - [X] 메트릭은 많이 바뀌지 않더라도 해당 요소는 많이 바뀔 것이므로 핵심 하위 도메인
        - [X] 메트릭은 바뀔 일이 잦지 않고, 중요도도 떨어지므로, 일반 하위 도메인으로 고려
    - [x] prometheus, prometheus wrapper, alertmanager 정책
        - [x] prometheus wrapper 를 가지고 갈 것인지
        - [x] alertmanager대신 notification service 사용에 대한 고민
        - [x] 위의 두 개의 사항은 각각의 통신에서 알림 전송을 잃어 버릴 수 있다는 문제점에 발생한 고민
- [x] biz 서비스 생성시 고려해야하는 것
    - [x] common영역에서 Actuator를 통해 health정보를 얻어오는 self call 을 하고 있는데, 이때에도 결국 비즈니스 영역에서 만든 security정책을 지켜야함.
        - [x] 그렇기 때문에 그것을 동적으로 처리할 수 있도록 정책을 잡고, 문서화를 해야함(mockup에도 적용시켜야함)
- [X] 전체 목표 재 고려
    - [X] 제품화 가능한 프레임워크 생성(솔루션이라고 생각했지만 구현하다보니 프레임워크가 됨)
    - [X] 프로젝트의 규모는 중-대규모의 구조를 가졌다는 것을 전제로 만들어짐
        - [X] 제품화
        - [X] 포트폴리오를 위함
        - [X] 미래 확장성 트레이닝
    - [X] 그러면서도 MVP는 놓치지 말기.
        - [X] MVP로 각각 단일 인스턴스 고려( ex) prometheus -> alertmanager : 해당 조합은 중-대규모에 맞지 않지만 MVP로는 맞음)
- [X] 2025-06-24 고려사항
    - [X] alertmanager와 prometheus 연합에 대한 고민 필요(HA구성 관련)
    - [X] S3 저장소 어떤거 사용할지에 대한 고려 필요
- [X] 2025-06-25 고려사항
    - [X] scyllaDB의 window에서 제약사항 확인 (제대로 된 세팅은 윈도우 이외의 환경에서만 가능)
    - [X] scyllaDB는 작업 스케쥴링을 윈도우의 것을 사용하는게 아닌 자체 스케쥴러(seastar) 사용
    - [X] scyllaDB는 저장소의 종류, 이외 여러가지 종류에 따라 세부 설정이 달라짐
- [x] 2025-07-10
    - [x] ScyllaDB에 대한 의구심 제기
        - [x] 정말 클라우드 환경에서 확장성과 유지보수성이 뛰어날 것인지
          (운영적 요소까지 고려해야함)
        - [x] 확장성을 추가할수는 없는지
          (각 biz 프로젝트의 특화된 쿼리 실행으로 원하는 로그를 가져 올 수 있도록 해야하는데, scyllaDB는 그것이 많이 부족해보임)
- [X] 2025-07-12
    - [X] ScyllaDB는 ACID 트랜잭션을 제공하지 않기 때문에, insert 후 kafka 메시지 전송 자체가 원 트랜잭션으로 묶이지 않는다는 한계점이 있다.
    - [X] 심지어 outbox transactional 패턴을 사용하기에도 애매한 것은 서로 다른 partitioning key를 사용하면 batch로 묶는 것 또한 비효율/비권장 된다는 점도 크다.
- [X] 2025-07-24 ~ 07-27
    - [X] logging service의 메트릭 정보, 그리고 로그의 error, warn 로그 등을 볼 수 있는 모니터링 서비스 구성에 대한 고민이 필요함.
    - [x] 코드 스멜 발생중. LogCollector, LogValidator, LogPersistence 이 세개는 하나의 인터페이스를 구현하도록 하고, 파이프 라인으로 동작하게 만들어서 worker는 각각의 파이프 라인을 잘 모르게 하고, config가 그것들을 worker에 제공해서 worker는 그것이 뭔지도 모르고 그저 하나씩 실행하는 구조로 만들어야 한다.
        - [x] worker.strategy=collect,validate,persistence 같은식으로 두기
        - [x] enum에서 각각에 대한 이름과 실제 plugin class를 묶어 넣기
        - [x] properties의 순서에 따라서 config의 List에 spec객체 담기
        - [x] enum 메서드에 `Map<String, String> setting`을 넣으면 spec 인스턴스 생성.
        - [x] worker 객체가 사용하도록, spec에서 순서대로 인스턴스 가져와서 list로 return.
        - [x] spec객체들에 대한 공통 상위 인터페이스 작성
        - [x] spec객체들에 대한 정적 팩토리 메서드 제거.
    - [x] implSpec annotation으로 상속에 대한 설명 적기
- [X] 2025-08-12
    - [X] 현재 프로젝트는 로깅 서비스와 알림 서비스 두 가지로 이루어져 있음.
    - [X] MVP 및 제대로 된 구성 을 위해 로깅 서비스 우선으로 모든 프로젝트의 초점을 맞출것.
    - [X] 그다음 모든 피트니스 및 구성이 완료된 다음 알림서비스 작업 진행
    - [X] 개발 테스트 환경 및 문서를 그에 맞출 준비 해야함
- [X] 2025-07-17~18 (logging-service 작업 전략)
    - [X] 거추장스럽고 무거운 Spring Boot 완전 제거
    - [X] 동작 전략 고민
        1. kafka consumer가 여러 개의 로그 데이터를 지정된 시간동안 가져오게 한다.
        2. 조건을 충족하는지 확인한다.
            1. 지정된 LOOP 횟수가 지났다면 바로 insert
            2. 지정된 개수 이상의 로그가 쌓였다면 insert
            3. 위의 두 조건중 하나라도 충족하지 못하면 1번으로
        3. insert 작업 진행
            1. 성공시 kafka에 commit 전달
            2. 실패시 kakfa에 rollback 전달
        - kafka에서 실패했다고 생각하는 시간을 초과하지 않는 선에서 모든 작업을 완수할 수 있도록 시간을 잡는다.
        - postgreSQL에서 subpartition으로 서비스를 지정하는 것은 현재 동작하지 않는다.
    - [x] 문제점 고민
        - [x] INSERT 실패시 문제가 있는 로그만을 어떻게 알아내서, 해당 요소만 제거한 후 재시도 할 수 있는지 (내부 구조 고민에 우회 정책 삽입)
        - [x] 실패한 로그를 어떻게 저장할지
        - [x] 셧다운 시그널로 graceful shutdown 전략 고민
        - [x] 인스턴스가 많아지면, 서로가 메시지를 가져가려고 하기 때문에 모두가 조건을 만족하지 못하고 시간에 따른 commit만 가능하다는 점, 이전 로그가 나중에 insert될 수 있다는 점 고민 필요
    - [X] 복제 전략
        1. kafka에 prometheus가 접근할 수 있도록 하여 메트릭 정보를 가져온다.
        2. HPA(kubernetes HorizontalPodAutoscaler)는 Prometheus Adapter에 요청을 날린다.
        3. adapter는 그 요청을 받아서 prometheus 내부 DB에서 메트릭 정보를 전달한다.
        4. HPA에 설정 된 대로 복제정책을 진행한다.
    - [X] 내부 구조 고민
        - [X] 멀티스레드로 동작하게 하고, 최대로 받을 로그의 양과 최대로 얼마동안 데이터를 수집할지 고민한다.
        - [X] insert 전에 작업해야할 내용은 다음과 같다.
            - 로그 데이터를 DTO에 담기
            1. 로그 데이터에 대한 검증 진행 (필수 값 여부와 데이터 타입 검증)
            2. 로그 데이터 필드명 변경 (.으로 되어있는 요소를 \_로 변경)
            3. 기본 정보값을 제외한 요소는 extra 필드에 JSON 형식으로 보유
        - [X] postgreSQL에서 나중에 들어온 데이터가 INSERT될 때 성능 이슈가 크게 없도록 PK값 고민
        - [X] graceful shutdown 구현해야하는 요소
            1. kafka poll 후 DB insert 여부에 따른 consumer 처리
            2. 메시지 처리 중 예외 발생 후 종료
            3. 멀티 스레드 환경에서 일부 작업 중단
        - [x] 결국 안정성을 높이기 위해 각 로그마다 id를 생성하도록(UUIDv7, ULID(멀티스레드 환경 우세)) 관련 로직을 provider에 추가 하고, postgreSQL에서 insert할때 `ON CONFLICT (log_id) DO NOTHING` 같은 식으로 처리하여, 다시 insert되지 않도록 한다.- [ ] 2025-07-19 (문서 관리 고민 사항)
    - [X] 현재 작성되어있는 모든 문서들을 old영역에 백업해두고, 제대로 관리할 수 있는 환경을 다시 만들어야함
        1. 제일 크게 그것을 느낀 이유는 결정문서(ADR) 때문임
        2. ADR이 너무 과하게 작은 개념에도 작성이 되어있어, 제안 요청에 들어갈 만한 요소들까지 포함되어있음
        3. 제안 요청서가 아예 없었기 때문이고, 사실상 계속 검증하고 고민할 시간이 필요한 요소들이 대부분이었는데, 바로 결정문서로 만들어 버림으로써, 불필요한 문서들이 계속 남아있게 되고, 전체 문서화 구성이 복잡해지게 되었음
        - 결국 다음과 같은 작업이 필요하다고 판단됨
            - [X] 결정을 내리기 전 제대로 확인할 수 있는 작업 제안서 항목 추가
            - [X] 결정문서 모두 정리하고, 아키텍처 문서와 합치기
            - [X] 개인 고민, 임시 문서, 플랜 문서 전부 정리하고 백로그 추가
    - [X] 시스템을 이해하기 위해 필요한 모든 문서들을 모아둔 상위 폴더 필요
        - **architecture 같은 문서는 참고용 문서가 아니기 때문에 따로 보관**
        - Contracts, buides, specs, checklists 같은 문서들은 하나로 묶어서, 전체 범주를 묶는게 좋아보임
        - 폴더 명은 /operational-docs 로 두는 것이 제일 좋아보임